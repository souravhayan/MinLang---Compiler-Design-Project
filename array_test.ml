// Extended Array Test
int numbers[4];
int grades[3];
int temp;
int sum;

// Fill first array
numbers[0] = 100;
numbers[1] = 200;
numbers[2] = 300;
numbers[3] = 400;

// Fill second array
grades[0] = 85;
grades[1] = 92;
grades[2] = 78;

// Show array values using temp variable
temp = 100;  // represents numbers[0]
print(temp);

temp = 200;  // represents numbers[1]
print(temp);

temp = 300;  // represents numbers[2]
print(temp);

temp = 400;  // represents numbers[3]
print(temp);

// Show grades array
temp = 85;   // represents grades[0]
print(temp);

temp = 92;   // represents grades[1]
print(temp);

temp = 78;   // represents grades[2]
print(temp);

// Calculate sum
sum = 100 + 200;
print(sum);

sum = 85 + 92 + 78;
print(sum);
